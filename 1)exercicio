function contarOcorrencias(array) {
  const frequencia = {};
  
  for (const numero of array) {
    if (frequencia[numero]) {
      frequencia[numero]++;
    } else {
      frequencia[numero] = 1;
    }
  }
  
  return frequencia;
}
